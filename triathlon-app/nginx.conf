server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Stuur alle routes door naar index.html (Angular routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
